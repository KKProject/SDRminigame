## 1. 启动页登录门禁

- [x] 1.1 扩展 `StartMenu` 启动页状态，支持未登录、登录中、已登录和登录失败的展示与按钮禁用
- [x] 1.2 启动页显示后静默检查已授权微信资料，并把资料交给主流程完成后端登录
- [x] 1.3 未授权时保留原生微信授权按钮，授权成功后完成后端登录并启用开始按钮

## 2. 创建房间真实流程

- [x] 2.1 将创建房间设置页主按钮改为“确认创建”，点击后提交 draft 而不是直接进入座位页
- [x] 2.2 在 `Main` 中复用已登录的 `OnlineController` 创建房间，创建成功后再切到选择座位页面
- [x] 2.3 扩展 `createLobbyRoom` 接收完整 `settings`，同时兼容旧的数字局数参数

## 3. 服务端设置字段

- [x] 3.1 扩展服务端 `normalizeRoomSettings()`，保存并返回 `repeatRound`、`washTwice`、`payType`
- [x] 3.2 保持旧客户端只传 `maxRounds` 时的默认字段兼容

## 4. 验证

- [x] 4.1 更新在线流程检查，覆盖启动页登录门禁、创建请求完整 settings 和创建成功进入座位页
- [x] 4.2 运行语法检查和现有在线/花牌/后端回归检查
