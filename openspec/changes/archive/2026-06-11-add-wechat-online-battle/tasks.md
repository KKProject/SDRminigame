## 1. 云开发基础设施

- [x] 1.1 在微信开发者工具开通云开发，确定云环境 ID，并在 `project.config.json`/`game.json` 配置云能力
- [x] 1.2 在客户端启动流程加入 `cloud.init`（仅在线模式初始化），确保不影响单机模式
- [x] 1.3 创建云数据库集合 `users`、`rooms`、`matchQueue`，并按设计配置安全规则（公共状态可读、私密手牌按 OPENID 限制）
- [x] 1.4 搭建云函数目录结构与公共依赖（`wx-server-sdk`），约定云函数命名与部署方式

## 2. 权威逻辑移植（server-game-engine）

- [x] 2.1 把 `js/game/rules.js` 纯规则常量与工具抽取为框架无关核心，移植为云函数可用的 CommonJS 模块
- [x] 2.2 把 `js/game/evaluator.js`（合法出牌、吃/碰/招/踏、胡牌判定、支招对子、刻子、吃锁、计分）移植到服务端核心
- [x] 2.3 把 `js/game/engine.js` 中的状态机推进逻辑（开局、发牌、阶段切换、接庄、结算）抽取为服务端权威引擎
- [x] 2.4 把 `js/game/ai.js` 决策逻辑移植到服务端，供补位/托管/单机复用
- [x] 2.5 编写服务端单测，用相同牌面与操作序列断言服务端结果与既有本地规则结果一致
- [x] 2.6 在牌桌状态中加入版本号，实现基于过期版本操作的拒绝（并发一致性保护）

## 3. 登录与玩家资料（wechat-auth）

- [x] 3.1 实现 `login` 云函数：用 `wx-server-sdk` 取 OPENID，写入/更新 `users` 记录并返回身份
- [x] 3.2 客户端实现 `wx.login` 调用与登录失败重试提示，登录成功前阻止进入在线匹配
- [x] 3.3 接入微信小游戏当前可用的头像昵称获取能力，拒绝授权时回退默认头像与默认昵称
- [x] 3.4 客户端提供后续补填头像/昵称入口，并把资料同步到服务端用户记录

## 4. 匹配与房间（online-matchmaking）

- [x] 4.1 实现快速匹配云函数：入队 `matchQueue`、撮合满人后创建 `rooms` 牌桌并分配座位
- [x] 4.2 实现取消匹配：把玩家移出队列并防止被本次撮合分配
- [x] 4.3 实现好友房间：创建房间码、凭码加入、校验不存在/已满错误
- [x] 4.4 实现牌桌生命周期（等待/进行中/结算/关闭）与座位归属（一人一座）、结束后释放资源
- [x] 4.5 实现人数不足时由托管 AI 补位开局的策略

## 5. 实时状态同步（realtime-state-sync）

- [x] 5.1 设计牌桌公共状态文档结构（阶段、当前行动席、各席公开资料/分数、最近弃牌、将牌、待响应动作摘要、版本号）
- [x] 5.2 服务端在每次状态推进后写入公共状态文档
- [x] 5.3 客户端用云数据库 `watch` 订阅公共状态并写入本地状态镜像、触发渲染
- [x] 5.4 实现私密手牌按 OPENID 鉴权的下发通道，保证公共状态不含他人手牌
- [x] 5.5 实现操作意图上报云函数（出牌、吃/碰/招/踏/胡/过、接庄/不接庄）并返回校验结果
- [x] 5.6 实现意图被拒绝时客户端保持权威状态显示不变并展示拒绝提示
- [x] 5.7 实现断线重连：拉取最新公共状态与本人手牌并重新订阅；服务端保留掉线玩家状态、超时按托管处理

## 6. 客户端薄客户端化（huapai-table-interaction / huapai-ai-opponents）

- [x] 6.1 重构 `js/databus.js` 为服务端公共状态 + 本人手牌的本地镜像（在线模式经 `setRoundState` 写入；单机保留本地引擎作离线回退）
- [x] 6.2 重构 `js/game/engine.js` 为薄客户端控制器（在线由 `js/net/online.js` 控制器负责登录/房间、订阅状态、上报意图、写镜像；`engine.js` 保留供单机）
- [x] 6.3 调整 `js/game/input.js`：触摸触发的是「上报意图」而非本地改状态（在线触摸在 `OnlineController.handleTouch` 上报 op）
- [x] 6.4 渲染器改用服务端下发资料展示对手头像/昵称，本人席使用本人微信资料（昵称已用服务端资料；头像图片绘制待办）
- [x] 6.5 客户端展示服务端控制的 AI 回合/思考进度（不在本地推断）
- [x] 6.6 新增主菜单「在线对战 / 单机练习」入口，单机模式复用移植后核心作为离线回退

## 7. 联调、回归与灰度

- [x] 7.1 真机联调登录、匹配、开局、出牌/吃碰招踏胡、结算、断线重连全链路
- [x] 7.2 验证手牌保密（其他客户端无法读到他人手牌）与非法操作拒绝
- [x] 7.3 复跑现有布局/渲染自检脚本，确认薄客户端化未破坏横屏/安全区/手牌列表现
- [x] 7.4 配置在线入口的灰度开关与一键回退到单机模式
- [x] 7.5 评估云开发配额与成本，优化文档体积与写入频率
