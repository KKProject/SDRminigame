## 1. 服务端最终结果状态机

- [x] 1.1 为最大局数最终结果初始化房主 15 秒决策窗口。
- [x] 1.2 在管理房间入口检查房主决策超时并关闭房间。
- [x] 1.3 支持 `requestRematch` 的接受与拒绝，拒绝者退出、人数不足时关闭房间。
- [x] 1.4 修复 `leaveRoom` 在结果状态漂移时被拒绝的问题。

## 2. 客户端交互

- [x] 2.1 最终结果页按 `host-decision/pending` 展示退出、继续、接受、拒绝按钮。
- [x] 2.2 客户端根据房主决策截止时间自动回大厅。
- [x] 2.3 客户端在服务端返回关闭、离开或拒绝结果时稳定回大厅。

## 3. 验证与收尾

- [x] 3.1 增加在线回归测试覆盖超时、退出、接受、拒绝和状态漂移退出。
- [x] 3.2 运行在线、后端和核心检查。
- [x] 3.3 同步主规格并归档变更。
- [x] 3.4 提交本地 commit。
