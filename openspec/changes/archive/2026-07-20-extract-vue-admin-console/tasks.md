## 1. 管理端工程基础

- [x] 1.1 在 `services/admin-web` 初始化 Vue 3、Vite、TypeScript 工程，配置 `/admin/` base、开发服务器和 `/api` proxy
- [x] 1.2 安装并配置 Vue Router、Pinia、Element Plus、Lucide 图标及按需导入，建立可复现的 lockfile
- [x] 1.3 配置 TypeScript、代码检查、Vitest、Vue Test Utils 和生产构建脚本，并将管理端检查接入项目级验证入口
- [x] 1.4 建立 design tokens、全局样式、字体回退、响应式断点、可见焦点和 `prefers-reduced-motion` 基础规则

## 2. API 与会话基础设施

- [x] 2.1 为现有 `/api/admin/*` 请求和响应定义 TypeScript 类型，并实现统一 API client、错误映射和请求中状态
- [x] 2.2 实现基于 `sessionStorage` 的 Pinia 会话 store，覆盖登录、`/api/admin/me` 身份恢复、退出和 401 清理
- [x] 2.3 配置 `/admin/` 路由、登录守卫和 `superadmin` 角色守卫，确保未授权或无权限访问被安全重定向
- [x] 2.4 为 API client、会话恢复、401 处理和角色路由守卫编写单元测试

## 3. 管理端布局与登录体验

- [x] 3.1 实现登录页、账号密码表单、字段校验、提交状态和靠近字段的可访问错误反馈
- [x] 3.2 实现深色侧栏、浅色工作区、管理员身份区、响应式导航和退出操作的应用布局
- [x] 3.3 使用一致的 Lucide SVG 图标、交互状态和中文文案，验证 375px、768px、1024px、1440px 视口无非预期整页横向滚动
- [x] 3.4 为登录成功、登录失败、键盘焦点和移动导航编写组件测试

## 4. 数据概览与危险操作

- [x] 4.1 实现集合概览卡片、集合表格、刷新操作及加载、空、成功和失败状态
- [x] 4.2 实现可复用危险操作确认对话框，支持焦点管理、确认文本校验、关闭恢复和提交防重
- [x] 4.3 接入单集合与全部集合清理 API，展示影响范围和删除结果，并在完成后刷新计数
- [x] 4.4 为集合渲染、确认文本、重复提交防护、成功刷新和错误重试编写组件测试

## 5. 管理员账号维护

- [x] 5.1 实现仅 `superadmin` 可见的管理员列表，展示角色、状态、创建人和时间信息及加载/空/错误状态
- [x] 5.2 实现新增管理员表单、用户名与密码规则提示、角色选择和服务端错误反馈
- [x] 5.3 实现禁用管理员确认对话框，并根据初始管理员、当前账号和启用状态禁用不合法操作
- [x] 5.4 为角色可见性、新增表单、受保护账号和禁用流程编写组件测试

## 6. 后端管理员安全初始化

- [x] 6.1 在后端配置层读取 `INITIAL_ADMIN_USERNAME` 和 `INITIAL_ADMIN_PASSWORD`，确保配置对象、错误和日志不输出密码值
- [x] 6.2 重构管理员初始化：仅在 `adminUsers` 为空时校验环境配置并创建受保护的 `superadmin`，已有任意管理员时不创建或覆盖账号
- [x] 6.3 将禁用保护从固定用户名判断改为数据库 `defaultAdmin` 标记判断，同时保持禁止管理员禁用自己
- [x] 6.4 移除页面、源码、示例配置和测试中可用的硬编码默认凭据，示例仅保留变量名和不可用占位符
- [x] 6.5 增加后端回归测试，覆盖空集合成功初始化、缺少或非法配置拒绝启动、错误脱敏、已有管理员不覆盖和受保护管理员不能禁用

## 7. 页面解耦与契约回归

- [x] 7.1 删除 `services/backend/src/admin-page.js` 及 `server.js` 中的内嵌页面导入和 HTML 响应逻辑
- [x] 7.2 调整后端回归测试，不再断言 Node 返回管理页面，并继续覆盖登录、身份、管理员维护、集合状态和集合清理 API
- [x] 7.3 增加管理端针对现有 API fixture 的契约测试，确认请求路径、字段、授权头和关键响应结构保持兼容
- [x] 7.4 运行管理端测试与生产构建、后端回归及项目现有在线核心检查，修复全部回归

## 8. 静态托管与部署流程

- [x] 8.1 只读核对生产 Nginx 现有站点配置和静态目录，确定不暴露密钥的管理端发布目录与回滚方式
- [x] 8.2 提供 Nginx `/admin` 到 `/admin/` 重定向、SPA `try_files`、静态资源缓存和现有 `/api`、`/ws` 反代兼容配置
- [x] 8.3 扩展部署脚本以构建、同步并原子切换版本化管理端产物，支持仅检查、仅管理端部署和失败回滚
- [x] 8.4 更新公网与服务器本机冒烟检查，验证入口 HTML、带 hash 静态资源、子路由回退和 `/healthz`，且不执行真实危险操作
- [x] 8.5 更新 `.env.example`、README 和 OpenSpec 运维上下文，记录安全初始化、独立构建部署、检查与回滚步骤，严禁写入真实凭据

## 9. 迁移与验收

- [x] 9.1 部署前备份并确认生产 `adminUsers` 状态；仅在集合为空且获得明确部署授权时临时设置初始化变量
- [x] 9.2 按“兼容后端、静态产物、Nginx 切换、最终后端”的顺序执行生产迁移并逐步验证，任何密钥检查只输出存在性
- [x] 9.3 在生产环境验证登录、会话恢复、集合计数、角色导航和管理员列表等非破坏性流程
- [x] 9.4 由维护者明确确认后验证清理与禁用等危险流程，记录结果但不记录 token、密码或数据库敏感内容
- [x] 9.5 验证旧硬编码凭据不能用于新初始化路径，确认仓库、构建产物、公网页面和运行日志均不暴露可用默认凭据
