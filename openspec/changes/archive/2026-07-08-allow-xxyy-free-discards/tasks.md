## 1. 规则判断

- [x] 1.1 梳理当前同句出牌合法性 helper，确认 `xxyy` 与 `xxyyz` 的判定分支不会互相污染。
- [x] 1.2 在服务端 evaluator 中增加 `xxyy` 开局双对自由拆打判断，允许同句连续出牌直到该句牌打完。
- [x] 1.3 保持 `xxyyz` 先打第三 key 后停止的既有行为不变。

## 2. 客户端同步

- [x] 2.1 在客户端 evaluator 中同步 `xxyy` 自由拆打判断。
- [x] 2.2 确认前端可选牌提示与服务端裁决保持一致。

## 3. 测试与验证

- [x] 3.1 增加服务端核心规则测试，覆盖 `xxyy` 可打任一对子 key，并可连续打到该句牌打完。
- [x] 3.2 增加边界测试，确认 `xxyyz` 先打第三 key 后仍不能继续打该句牌。
- [x] 3.3 增加或更新客户端规则自检，覆盖 `xxyy` 自由拆打。
- [x] 3.4 运行语法检查、服务端核心规则、客户端自检和在线流程回归。
