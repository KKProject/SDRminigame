## 1. 服务端响应窗口

- [x] 1.1 在服务端引擎中新增可持久化的并发 `responseWindow` 状态，记录窗口来源、候选动作、每席响应状态和选择结果
- [x] 1.2 将出牌/摸牌后的响应入口改为创建并发响应窗口，并为 AI、空座、离线或托管席位快速写入自动选择或过牌
- [x] 1.3 实现响应选择提交、合法性校验、forced 过牌处理和同窗口重复/过期选择拒绝
- [x] 1.4 实现“当前最佳选择是否可被未决候选推翻”的裁决函数，支持招/踏在无胡候选时提前裁决
- [x] 1.5 移除响应窗口内逐个 `pass` 公开事件，改为仅在最终无人响应时生成归位事件

## 2. 房间同步与私密视图

- [x] 2.1 调整 `buildPublicState` 和 `buildPrivateView`，公共状态只暴露响应摘要，私密视图只下发本人响应按钮
- [x] 2.2 调整房间层动画屏障逻辑，`await-response` 入场动画完成后开放并发响应窗口，私有过牌不建立新屏障
- [x] 2.3 修复掉线/超时响应阶段托管推进，使离线或超时席位不会阻塞响应窗口
- [x] 2.4 保持 WebSocket `op`、`pull`、`ackAnimation` 返回快照与版本语义一致，并处理提前裁决后的过期响应错误

## 3. 客户端响应交互

- [x] 3.1 调整在线状态映射，从私密视图读取本人 `playerActions`，不再依赖公共 `pub.playerActions`
- [x] 3.2 渲染并发响应等待摘要，保持出现牌可见且不泄露其他玩家具体动作
- [x] 3.3 在服务端裁决、窗口取消或动作失效时自动收起本地响应弹窗并展示轻量反馈
- [x] 3.4 确认本地动作预演只用于最终胜出的权威事件，过牌不触发公开动作动画
- [x] 3.5 在线响应类凑牌/胡/过动作不再播放本地乐观预演，统一等待服务端裁决事件后播放权威动画

## 4. 验证与回归

- [x] 4.1 增加服务端核心测试：多真人同时响应、同级座位顺序、招/踏提前裁决、胡拦截招/踏、全部过牌归位
- [x] 4.2 增加 AI/托管测试：AI 响应窗口自动选择、离线/超时席位自动过牌、forced 过牌进圈
- [x] 4.3 增加在线同步测试：私密响应按钮不进公共状态、提前裁决后旧响应被拒绝、公共摘要不泄露候选动作
- [x] 4.4 运行 `node scripts/run-server-core-checks.mjs`、`node scripts/run-online-checks.mjs` 和相关花牌/动画回归检查
