## 1. 建立运行时渲染指标管理

- [x] 1.1 在 `render.js` 中抽取候选窗口指标读取、横屏有效性校验、稳定签名和连续确认逻辑
- [x] 1.2 将启动期不可变渲染指标改为可查询、可订阅的当前稳定指标，并保留现有计算函数兼容性
- [x] 1.3 实现稳定指标提交，原子更新 Canvas backing store、2D context transform、安全区和当前指标快照
- [x] 1.4 对无效、纵屏过渡和重复相同候选实现不提交策略，保证最后稳定指标不被覆盖

## 2. 衔接主程序窗口生命周期

- [x] 2.1 在主程序初始化时注册一次 `wx.onWindowResize`，并在收到通知后重新读取和确认窗口指标
- [x] 2.2 在首次指标未稳定期间按有限重试策略重新读取，并阻止正式菜单与牌桌交互布局创建
- [x] 2.3 在稳定后停止持续轮询，确保重复尺寸通知不会重复重置 Canvas 或布局
- [x] 2.4 为不支持 `onWindowResize`、`getWindowInfo` 或返回异常数据的微信基础库保留兼容兜底

## 3. 更新菜单、布局与渲染器

- [x] 3.1 修改启动菜单，使绘制、按钮命中区域和原生微信授权按钮始终使用最新稳定逻辑尺寸与安全区
- [x] 3.2 为 `TableLayout` 增加运行时 viewport 与安全区更新入口，并在稳定尺寸变化后重新计算全部布局区域
- [x] 3.3 为 `TableRenderer` 增加稳定指标变化处理，更新布局并保证背景继续覆盖完整 Canvas
- [x] 3.4 验证手牌稳定列身份只因屏幕变化重新计算坐标，不因重布局改变牌列顺序

## 4. 处理动画与输入恢复

- [x] 4.1 在稳定尺寸变化时取消旧坐标下的活动 Tween、临时视觉和旧完成回调
- [x] 4.2 使用最新牌局状态与新布局恢复等待响应牌、静态弃牌、凑牌和动画目标
- [x] 4.3 保证重复相同尺寸通知不会取消当前动画、重复音效或重复动画完成通知
- [x] 4.4 验证单机与在线输入命中测试在尺寸更新后只使用最新布局区域

## 5. 自动检查与真机验收

- [x] 5.1 扩展渲染自检，覆盖异常首帧后稳定横屏、稳定后异常候选、重复通知、安全区变化和高像素比
- [x] 5.2 扩展布局与动画自检，验证尺寸变化后的元素分布、背景全屏、动画恢复和命中区域
- [x] 5.3 运行现有规则、动画、在线和 OpenSpec 严格校验，修复回归
- [ ] 5.4 在曾复现问题的真机上验证冷启动、切后台恢复和横屏方向变化时元素不再挤在一起
