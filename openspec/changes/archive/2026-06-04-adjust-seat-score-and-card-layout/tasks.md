## 1. 福数与玩家状态数据

- [x] 1.1 增加当前局已操作牌福数计算工具，复用现有颜色、招、踏和将牌倍数规则。
- [x] 1.2 在每个玩家状态或渲染派生数据中提供累计总输赢点数和当前局已操作牌福数。
- [x] 1.3 确保新局开始时当前局已操作牌福数归零，且只统计玩家自己的公开操作牌组。

## 2. 头像与点数布局

- [x] 2.1 在 `js/game/layout.js` 中增加四方头像区域、总分文本区域、本局福数文本区域。
- [x] 2.2 将对家头像定位到顶部居中，上家头像定位到左上角，下家头像定位到右上角，自己头像定位到左下角。
- [x] 2.3 在 `js/game/renderer.js` 中绘制正方形有色头像占位和头像下方两行点数。
- [x] 2.4 保证头像、点数、mini 牌区、手牌和动作弹窗不发生关键重叠。

## 3. 我的手牌拆列规则

- [x] 3.1 重构手牌列构建逻辑，使同一句话可生成多个相邻牌列且每列最多 6 张牌。
- [x] 3.2 实现超过 6 张时优先拆出当前数量最多的字作为独立牌列。
- [x] 3.3 实现剩余牌不超过 6 且包含多个字时按原句字序保留在同一相邻牌列。
- [x] 3.4 实现剩余牌仍超过 6 时继续按最多字优先拆列。
- [x] 3.5 实现单字牌集中到最后牌列，超过 6 张时继续拆成相邻最后列。
- [x] 3.6 确保打牌或凑牌导致空列后，剩余列自动压紧且不改变相对顺序。
- [x] 3.7 保持手牌点击命中、选中状态和再次点击打出逻辑稳定。

## 4. 凑牌与弃牌摆放

- [x] 4.1 在布局中增加四方凑好牌区域，并按自己、对家、下家、上家的指定位置和方向输出。
- [x] 4.2 在布局中增加四方弃牌/打牌区域，并按自己、对家、下家、上家的指定位置和方向输出。
- [x] 4.3 在渲染中按区域方向绘制凑好牌 mini 图，mini 图之间不留缝隙。
- [x] 4.4 在渲染中按区域方向绘制弃牌/打牌 mini 图，mini 图之间不留缝隙。
- [x] 4.5 保留背景优先原则，不为这些牌区绘制常驻面板或底色。

## 5. 验证

- [x] 5.1 更新 `scripts/run-huapai-checks.mjs`，覆盖头像点数区域在屏内且不遮挡关键牌区。
- [x] 5.2 增加手牌拆列测试：超过 6 张拆列、最多字优先、剩余牌合并、单字收集和空列压紧。
- [x] 5.3 增加凑牌与弃牌方向测试，确认从左到右或从右到左排列且 mini 图无额外缝隙。
- [x] 5.4 增加当前局已操作牌福数测试，覆盖新局归零、吃牌增加 1 福、碰/招/踏加福数。
- [x] 5.5 运行 `node scripts/run-huapai-checks.mjs` 并修复回归。
- [x] 5.6 运行 `openspec validate adjust-seat-score-and-card-layout` 确认变更有效。
