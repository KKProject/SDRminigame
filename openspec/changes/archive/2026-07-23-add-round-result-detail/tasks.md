## 1. 服务端结果详情协议

- [x] 1.1 在权威牌局进入 `phase=result` 时构建按当前局冻结的 `roundDetail`，包含四家最终手牌、公开牌组、本局分数、结果类型和局数信息
- [x] 1.2 将 `result.scoring.totalFu` 仅映射为胡牌赢家的 `huCount`，其余座位保持空值，并保证非结果阶段公共状态不包含对手完整手牌
- [x] 1.3 让 HTTPS 快照、WebSocket snapshot/delta 和重连恢复一致下发 `roundDetail`，下一局开始时清除上一局详情

## 2. 全员继续下一局

- [x] 2.1 为普通 `finished` 房间增加按局号绑定的真人继续确认状态和幂等确认入口，AI 不进入所需确认集合
- [x] 2.2 在最后一名所需真人确认时原子启动下一局，并防止重复请求、迟到局号或并发请求导致重复开局
- [x] 2.3 将确认摘要广播给所有房间客户端并支持断线重连恢复；最大局数结果禁止普通继续确认

## 3. 客户端结果状态接入

- [x] 3.1 扩展 DataBus 和在线快照/增量映射，旋转并保存四家 `roundDetail`、本人确认状态和确认进度
- [x] 3.2 将全屏结果页提交接入现有终局动画时间线闸门，确保胡牌、进圈、流局或荒庄动画完成后才显示详情
- [x] 3.3 接入“继续下一局”的幂等提交与等待状态；最终局“查看战绩”点击后仅显示待开放反馈且不启动新局

## 4. 全屏对局结果视觉

- [x] 4.1 准备并接入参考图风格的红金标题、米色主面板、玩家行、角色标签和唯一主按钮资源，同时提供资源缺失时的 Canvas 可读降级
- [x] 4.2 实现横屏安全区内的四行结果布局，展示头像昵称、最终手牌、吃碰招踏牌组、胡数和本局分数，并高亮本家及胡牌玩家
- [x] 4.3 根据局数只显示“继续下一局”或“查看战绩”，移除结果页累计得分、分享战绩、保存回放和返回大厅入口
- [x] 4.4 在常规横屏、刘海屏和最窄支持安全区验证牌面缩放、四行信息和按钮命中区域无重叠
- [x] 4.5 增加玩家结果条目高度以容纳固定 mini 牌纵向分列，并为四家结果列表接入主面板内的纵向拖动与滚动提示
- [x] 4.6 将滚动视口左右边界与结果背景空白主面板对齐，并在刘海安全区设备上保持可操作

## 5. 回归验证

- [x] 5.1 补充服务端测试，覆盖进行中手牌脱敏、结果阶段四家牌况、赢家胡数、非胡牌结果空胡数及下一局清理
- [x] 5.2 补充多人房间测试，覆盖部分确认等待、全员确认自动开局、AI 不阻塞、重复确认、迟到确认和最终局禁止继续
- [x] 5.3 补充客户端测试，覆盖终局动画显示顺序、四家座位旋转、本家高亮、唯一条件按钮、确认重连恢复和查看战绩占位反馈
- [x] 5.4 运行 `node scripts/run-animation-checks.mjs`、`node scripts/run-online-checks.mjs`、`node scripts/run-server-core-checks.mjs` 和 `node scripts/run-backend-checks.mjs`
- [x] 5.5 上传微信小游戏测试版本，并在两真人两 AI 真机房间复测胡牌、进圈、流局、普通续局和最终局结果页
