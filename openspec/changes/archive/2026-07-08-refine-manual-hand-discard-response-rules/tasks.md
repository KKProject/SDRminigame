## 1. 规则状态建模

- [x] 1.1 梳理服务端与客户端当前 `actionHistory`、discard、draw 流转事件，确认哪些事件表示“开局手牌主动打出”。
- [x] 1.2 增加或收敛手牌主动打出 key 的读取 helper，语义命名为 `manualHandDiscardKeys` 或等价概念。
- [x] 1.3 确保抓牌后无人响应的自动流走、系统弃牌、动画弃牌不会写入或命中手牌主动打出记录。

## 2. 响应裁决

- [x] 2.1 更新服务端 chi 枚举与动作提交校验：玩家已从开局手牌主动打过同 key 时，不允许吃该 appearing card。
- [x] 2.2 更新服务端 hu 枚举与动作提交校验：玩家已从开局手牌主动打过同 key 时，不允许胡该 appearing card，不再区分 chi-style hu 和同张门 hu。
- [x] 2.3 接入强制吃冲突裁决：必须吃的 key 已在玩家手牌主动打出记录中时，直接产生 circle-loss，并按现有进圈赔三家结算。
- [x] 2.4 同步客户端 evaluator 的按钮过滤口径，避免展示服务端必然拒绝的吃/胡按钮。

## 3. 测试与验证

- [x] 3.1 增加服务端核心规则测试：开局手牌主动打出后，同 key 后续不能吃、不能胡。
- [x] 3.2 增加服务端核心规则测试：抓牌流走或非手牌事件不污染手牌主动打出记录，后续同 key 仍可按其他规则吃/胡。
- [x] 3.3 增加服务端核心规则测试：必须吃但已从开局手牌主动打过同 key 时触发 circle-loss，结算赔三家。
- [x] 3.4 更新客户端自检或共享规则测试，删除旧的“已打过同 key 仍可非 chi 胡”断言。
- [x] 3.5 运行相关本地回归，至少覆盖服务端核心规则、客户端规则自检和在线流程检查。
