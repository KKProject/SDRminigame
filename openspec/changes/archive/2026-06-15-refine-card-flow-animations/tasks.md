## 1. 扩展权威事件协议

- [x] 1.1 为抓牌和出牌公开事件增加 `appearanceResolution` 字段、序列化和兼容校验，并确保事件不泄露响应玩家与动作详情
- [x] 1.2 为吃、碰、招、踏公开事件补齐稳定 meld id、最终公开牌组和完整牌组动画所需数据
- [x] 1.3 补充公开事件模型测试，覆盖 `await-response`、`auto-discard`、完整 meld 数据和暗牌信息隔离

## 2. 调整服务端牌局推进

- [x] 2.1 修改抓牌和出牌流程，在事件创建时根据服务端规则计算结果选择 `await-response` 或 `auto-discard`
- [x] 2.2 对初始即无人响应的牌提前提交最终弃牌状态，并合并为一次出现牌复合事件，不再生成独立 `unclaimed` 事件
- [x] 2.3 保留“最初存在响应机会但所有玩家后来都过”的最终归位事件，并确保它在动画回执后才推进下一步
- [x] 2.4 调整动画继续令牌和屏障恢复逻辑，使等待响应分支在入场回执后开放操作，自动归位分支在完整复合动画回执后继续
- [x] 2.5 补充服务端流程测试，覆盖抓牌、出牌、初始无人响应、全部过、响应操作和重复回执

## 3. 提供动画终点与临时隐藏模型

- [x] 3.1 扩展布局层，为四个席位提供当前弃牌槽位的精确目标矩形、最终 mini 尺寸和方向
- [x] 3.2 扩展布局层，为最终凑牌 meld 提供完整牌组目标矩形、组内排列和最终 mini 尺寸
- [x] 3.3 在渲染器中按 card id 和 meld id 临时隐藏权威快照中的静态弃牌与凑牌，并在动画交接后可靠恢复
- [x] 3.4 增加布局与隐藏状态测试，验证四个席位的动画终点和静态显示完全一致

## 4. 实现出现牌复合动画

- [x] 4.1 抽取统一出现牌动画预设，实现大牌在出现位置的 `80% → 120% → 100%` 入场阶段
- [x] 4.2 实现 `await-response` 分支，将入场完成的大牌转为不占用 Tween 和动画屏障的保留出现牌
- [x] 4.3 实现 `auto-discard` 分支，使大牌在入场后立即缩小移动到最终弃牌槽位，并与静态 mini 弃牌无缝交接
- [x] 4.4 实现最终归位事件，使所有玩家选择“过”后保留出现牌能够缩小飞入弃牌区
- [x] 4.5 调整消耗操作和“过”的行为，确保吃、碰、招、踏、胡开始时立即移除保留牌，而单个“过”继续保留牌

## 5. 实现完整凑牌牌组动画

- [x] 5.1 按手牌分列规则和大图资源构建完整凑牌牌组动画模型，并用组容器保持子牌相对位置
- [x] 5.2 实现完整牌组在牌桌中央整体播放 `80% → 120% → 100%` 的展示阶段
- [x] 5.3 实现完整牌组整体缩小飞入目标凑牌区，并在到达后与最终静态 meld 无缝交接
- [x] 5.4 覆盖吃、碰、招、踏的不同牌数和四个席位方向，确保组动画位置、比例和方向正确

## 6. 衔接本地预演、权威事件与音效

- [x] 6.1 扩展动画状态控制器，使本地抓牌和出牌预演可在权威事件确认后继续对应出现牌分支而不重播
- [x] 6.2 扩展凑牌本地预演确认逻辑，使完整牌组动画和动作音效只播放一次
- [x] 6.3 调整动画回执时机：等待响应分支在入场并保留后回执，自动归位和凑牌分支在静态交接后回执
- [x] 6.4 完善取消、断线重连和权威事件不匹配恢复逻辑，清理活动动画、保留出现牌和临时隐藏状态

## 7. 联调与验收

- [x] 7.1 运行自动化测试和 OpenSpec 严格校验，修复事件序列、动画回执和静态交接相关回归
- [x] 7.2 在在线四人牌局中验证抓牌、出牌、有人响应、初始无人响应、所有人过、吃、碰、招、踏和胡的完整流程
- [x] 7.3 在四个席位和全面屏真机上验证动画比例、最终位置、无重复动画、无闪烁、无跳位以及操作期间无错误推进
