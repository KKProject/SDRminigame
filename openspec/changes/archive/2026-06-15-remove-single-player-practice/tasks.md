## 1. 审计练习模式删除边界

- [x] 1.1 扫描客户端入口、本地引擎、输入器、客户端 AI、自检、测试和文档的引用关系，确认练习专属模块与在线共享模块边界
- [x] 1.2 列出仍由在线流程使用的共享规则、布局、渲染、动画、服务端引擎和服务端 AI，确保后续删除不影响这些能力

## 2. 收敛为仅在线启动流程

- [x] 2.1 修改 `js/main.js`，删除客户端 `HuapaiEngine`、`TableInput` 的构造与 `single` 模式分支，只保留在线对战启动和渲染流程
- [x] 2.2 修改 `js/ui/menu.js`，删除“单机练习”按钮、非在线选择分支和离线回退逻辑，保留在线授权、状态提示和重试入口
- [x] 2.3 删除 `ONLINE_BATTLE_ENABLED` 练习回退开关，并在 `js/config.js` 无其他职责时删除该文件
- [x] 2.4 清理 `js/net/cloud.js` 等客户端代码中将在线初始化与单机练习并列描述的注释和死分支

## 3. 删除客户端练习牌局实现

- [x] 3.1 在确认无在线或共享引用后删除 `js/game/engine.js` 和 `js/game/input.js`
- [x] 3.2 在确认服务端 AI 独立保留后删除练习模式专属的 `js/game/ai.js`
- [x] 3.3 清理所有已删除模块的导入、实例字段、模式状态、事件绑定和不可达代码
- [x] 3.4 验证 `cloudfunctions/game/core/engine.js`、`cloudfunctions/game/core/ai.js` 及在线 AI 调度未被删除或改为客户端执行

## 4. 调整自检、测试和文档

- [x] 4.1 重构 `js/game/self-check.js` 和 `scripts/run-huapai-checks.mjs`，删除客户端引擎与客户端 AI 依赖，同时保留共享规则、评分、资源、布局和渲染覆盖
- [x] 4.2 将仍有价值且仅由已删除客户端 AI 测试覆盖的决策场景迁移到服务端核心测试
- [x] 4.3 更新 `README.md` 和当前文档，将项目描述改为仅在线对战，并删除单机练习、本地真人对三 AI 和离线回退说明
- [x] 4.4 全文扫描并清理 `single`、`单机练习`、`startSinglePlayer`、`ONLINE_BATTLE_ENABLED` 及已删除客户端模块的残留引用，同时排除“单牌”等无关业务术语

## 5. 验证仅在线运行

- [x] 5.1 运行共享花牌规则、自检和布局渲染测试，确认删除练习模式后共享能力仍通过
- [x] 5.2 运行动画、在线流程和服务端核心测试，确认在线操作、服务端 AI、空座补位与断线托管仍通过
- [x] 5.3 执行 OpenSpec 严格校验并确认当前规格不再要求单机练习能力
- [x] 5.4 在微信开发者工具验证首次授权、已授权、在线启动失败重试、在线开局与操作流程，确认不存在单机入口或自动离线回退
- [x] 5.5 在真机验证在线授权、开局、真人操作和服务端 AI 推进牌局，确认移除练习模式未影响正式玩法
