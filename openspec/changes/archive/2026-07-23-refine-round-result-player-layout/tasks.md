## 1. 冻结胡牌结果数据

- [x] 1.1 在服务端结果详情构造中，按权威 `result.doors` 从赢家冻结牌池生成带标签和稳定牌对象的 `winningGroups`
- [x] 1.2 在赢家详情中增加 `winningCard`，保持 `finalHand` 和完整胡牌分组仍包含该牌，并确保非赢家与非胡牌结果不携带伪造字段
- [x] 1.3 扩展服务端和在线结果检查，覆盖“吃 上大人”与“胡 上”双重语义、重复牌映射及非胡牌结果

## 2. 在线结果映射

- [x] 2.1 扩展 `rotateRoundDetail`，在旋转玩家 seat 时保留并复制 `winningCard`、`winningGroups` 和组内牌序
- [x] 2.2 增加 snapshot、result delta 与重连恢复检查，确认三条路径获得一致的权威胡牌详情

## 3. 结果页身份与牌型布局

- [x] 3.1 缩小结果页身份区和头像尺寸，将头像改为 1:1 正方形区域，并把昵称与身份标签纵向排列在头像下方
- [x] 3.2 将结果页 mini 牌宽高调整为原计算尺寸的约 `80%`，同步调整叠放间距并保留最窄横屏可辨认下限
- [x] 3.3 更新牌型区域宽度与列布局，使新增胡牌列和统计区在支持的横屏安全区内不重叠

## 4. 结果页胡牌绘制

- [x] 4.1 让赢家优先使用 `winningGroups` 绘制带“吃、碰、对”等标签的完整权威牌型，并为字段缺失保留现有展示回退
- [x] 4.2 在赢家完整牌型后追加“胡”列并再次绘制 `winningCard`，明确禁止与完整牌型去重
- [x] 4.3 将头像裁切和描边改为正方形或小圆角样式，居中绘制头像下方昵称并保持本家、赢家识别

## 5. 回归与验收

- [x] 5.1 更新结果页渲染检查，验证牌和叠放间距缩小、头像正方形、昵称下置以及非赢家不预留胡牌列
- [x] 5.2 增加“吃上胡”定向检查，验证同时存在完整“吃 上大人”和独立“胡 上”
- [x] 5.3 在多种横屏尺寸验证四家纵向滚动、统计区域、底部信息和主操作按钮不受影响
- [x] 5.4 运行胡牌、动画、在线、服务端核心、后端及 OpenSpec 全量校验并修复所有回归
- [x] 5.5 根据真机反馈将结果牌宽度从 `10～15px` 提高到约 `13～20px`，更新规格和回归检查并重新验证
- [x] 5.6 将每个结果牌列限制为最多 5 张，超过时按牌序均衡拆成最少列，并覆盖“上上上 / 大大人”和 5 张不拆分回归
- [x] 5.7 根据真机反馈将结果牌提高到原尺寸约 `80%`，并按同字门子边界拆分长列，覆盖“尔尔尔 / 小小小小”回归后直接部署
- [x] 5.8 修正结果页碰招踏标签：三张同字显示碰、四张及以上显示招、`ta` 显示踏，并覆盖服务端冻结结果和客户端旧数据回退
- [x] 5.9 修正胡牌二牌门标签：`xx`（如“上上”）显示“对”，`xy`（如“上大”）显示“口”，并覆盖服务端与客户端回归
- [x] 5.10 优化并接入赢家“胡”PNG和胡型竖牌背景，冻结 `huGrade`，在牌列右侧竖排显示平胡/小甲/大甲/场胡并覆盖布局、资源和同步回归
- [x] 5.11 修复线上只显示“胡”图的问题：赢家 `roundDetail.huGrade` 缺失时回退公共权威 `result.grade`，并确保非赢家不继承胡型
