## 1. 切图处理与包体基线

- [x] 1.1 记录当前 `images/` 资源占用和微信上传完整主包大小，确认旧 `round_result_bg.jpg` 的引用位置
- [x] 1.2 从 `/Users/wangyoukun/Desktop/tinified/` 导入标题、胜负状态和继续按钮切图，并验证尺寸、格式及 alpha 通道
- [x] 1.3 为白板生成多个保留 alpha 的尺寸/调色板候选，比较体积、边缘、渐变和高像素比清晰度后选定发布版本
- [x] 1.4 将选定切图放入项目资源目录并统一为稳定的英文语义文件名

## 2. 资源清单与绘制基础能力

- [x] 2.1 扩展结果页资源清单和加载器，暴露标题、白板、胜利、失败和继续按钮图片，并为单项加载失败保留无异常回退
- [x] 2.2 实现 Canvas 九宫格图片绘制辅助能力，支持独立源切线和目标矩形且不改变四角比例
- [x] 2.3 为白板确定四边保护区参数，并验证窄屏、宽屏及像素比 1/2 下没有明显拉伸接缝

## 3. 对局结果页重搭

- [x] 3.1 调整结果页布局，在安全内容区域内定位透明标题、九宫格白板、固定底部信息和唯一主操作按钮
- [x] 3.2 使用本机玩家视角映射胡牌结果的胜利/失败切图，进圈、流局和荒庄继续显示准确的 Canvas 状态文案
- [x] 3.3 在白板内部保留四家高条目、纵向叠放 mini 牌、本家高亮、胡牌角色、权威最终胡数和本局分数
- [x] 3.4 保留玩家列表纵向拖动、裁剪和滚动边界，确保标题、底部信息及按钮固定且拖动不会误触按钮
- [x] 3.5 未达到最大局数时使用继续切图呈现“继续下一局”；最终局保留 Canvas“查看战绩”回退且不接入“再来一局”切图
- [x] 3.6 为所有结果页切图补齐逐元素 Canvas 回退并确认资源缺失不会阻断结果页或下一局确认

## 4. 清理与验证

- [x] 4.1 确认新结果页不再引用旧整屏 JPG 后删除 `images/round_result_bg.jpg` 及其资源映射
- [x] 4.2 增补或更新自动化检查，覆盖资源映射、状态语义、九宫格绘制、滚动命中区域、胡数显示和最终局按钮回退
- [x] 4.3 在至少一种窄横屏和一种宽横屏尺寸下截图核对透明通道、白板两侧对齐、切图清晰度、四家滚动内容及按钮位置
- [x] 4.4 运行客户端及在线流程相关回归，确认下一局确认、最终局待开放反馈和现有结算动画时序未回退
- [x] 4.5 使用项目指定的 Node 20 上传流程检查 `subPackageInfo.__FULL__`，确保主包小于 4 MiB并尽量不高于约 3.7 MiB

## 5. 真机反馈修正

- [x] 5.1 将最终剩余手牌从按牌面 key 分列改为按 `phraseId` 归组，同一句不同牌面纵向叠放
- [x] 5.2 增加同一句叠放、不同句分列及 meld 独立列的自动化回归并运行相关检查
