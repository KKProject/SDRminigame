## 1. 服务端公开事件模型

- [x] 1.1 定义公开操作事件结构、事件类型、连续 `eventSeq`、动画屏障和继续令牌结构
- [x] 1.2 在服务端权威引擎中增加统一事件记录器与单步暂停机制，并在开新局和加载旧房间时初始化兼容字段
- [x] 1.3 为公开摸牌、出牌、无人响应、过、吃、碰、招、踏、接庄、不接庄、胡牌、进圈、流局和结算生成单步事件与后续继续令牌
- [x] 1.4 增加公开事件序列化过滤，确保事件不包含其他玩家未公开手牌牌面或 card id
- [x] 1.5 实现所有必需客户端回执后幂等执行继续令牌，并在动画等待期间拒绝新的牌局操作

## 2. 公共状态同步协议

- [x] 2.1 扩展 `roomStates` 公共文档，写入当前事件、最新事件序号、必需回执名单、已回执名单和截止时间
- [x] 2.2 扩展 `pull` 返回值，使客户端能够获得最新权威快照、当前事件和自身回执状态
- [x] 2.3 新增按 OPENID 鉴权且幂等的 `ackAnimation` 云函数操作
- [x] 2.4 实现动画回执超时检查，把未回执玩家标记掉线或托管并解除阻塞
- [x] 2.5 增加旧房间无动画屏障字段的兼容逻辑

## 3. 客户端在线事件消费

- [x] 3.1 在 `OnlineController` 中增加当前事件、最后已播放序号、最后已回执序号和动画播放状态
- [x] 3.2 实现当前事件去重、序号连续性检查和单事件播放调度
- [x] 3.3 动画完成后调用 `ackAnimation`，并对失败回执进行幂等重试
- [x] 3.4 实现首次进入和断线重连时根据自身回执状态决定播放当前事件或直接对齐快照
- [x] 3.5 动画等待期间锁定牌局操作，屏障解除后再开放服务端允许的操作
- [x] 3.6 确保本人服务端确认动作只播放一次，且不会与本地点击反馈重复

## 4. 渲染器逐操作动画队列

- [x] 4.1 为渲染器增加明确的在线事件动画启动、活动状态和完成回调接口
- [x] 4.2 将摸牌、出牌、无人响应和响应牌移动映射到现有大牌飞行动画与目标区域
- [x] 4.3 将吃、碰、招、踏、过、接庄、不接庄、胡牌、进圈、流局和结算映射到动作特效与结果展示
- [x] 4.4 保证一次只播放一个牌局事件，动画期间隐藏尚未落位的弃牌或凑牌 mini 图
- [x] 4.5 动画完成后触发一次完成回调并保持等待状态，直到服务端下发下一事件或操作机会

## 5. 自动化与真机回归

- [x] 5.1 扩展服务端核心测试，覆盖单步暂停、事件序号、继续令牌、动画等待期间拒绝操作和隐私过滤
- [x] 5.2 扩展在线同步测试，覆盖动画回执、重复回执、回执失败重试、断线恢复和本人动作去重
- [x] 5.3 扩展渲染测试，覆盖四个席位出牌、连续摸打、吃碰招踏、无人响应、结算串行动画和无重叠
- [x] 5.4 增加服务端回执超时测试，验证未回执客户端进入掉线托管后牌局继续推进
- [ ] 5.5 真机验证真人与 AI 每个动作均在所有在线客户端动画完成回执后才继续，且操作弹窗、全面屏安全区和断线重连不受影响
- [ ] 5.6 监测动画回执耗时、超时率、云函数调用量和数据库写入量，确定最终回执截止时间
