## 1. 管理接口与数据库适配

- [x] 1.1 在配置中加入 `ADMIN_TOKEN` 并更新环境变量示例。
- [x] 1.2 为 Memory/File/Mongo 数据库集合适配层增加计数和清空能力。
- [x] 1.3 新增受 `ADMIN_TOKEN` 保护的管理 API，支持状态查询和白名单集合清空。

## 2. 管理页面

- [x] 2.1 新增 `/admin` 页面，展示集合数量、用途说明和刷新操作。
- [x] 2.2 增加单集合删除、全部清空和二次确认交互。
- [x] 2.3 页面读取 URL token 后移除地址栏 token，并使用 `Authorization: Bearer` 调用 API。

## 3. 验证与归档

- [x] 3.1 扩展后端检查覆盖未配置、未授权、查询计数、拒绝非白名单和清空集合。
- [x] 3.2 运行相关检查并通过浏览器或 HTTP smoke 验证管理页面。
