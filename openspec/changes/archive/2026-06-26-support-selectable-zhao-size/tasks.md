## 1. 规则建模

- [x] 1.1 梳理当前 `findZhaoActions()`、`filterHighestPriority()`、`applyMeldCards()`、`applyAction()`、AI 响应选择和在线动作提交的调用链
- [x] 1.2 为 `zhao` 动作明确载荷字段：目标招牌大小、消耗手牌 key 数量，或稳定等价字段，并保证服务端可重新校验
- [x] 1.3 调整招牌动作枚举：手里同 key 数量为 3/4/5 时分别生成 1/2/3 个候选招牌动作
- [x] 1.4 按每个候选招牌动作实际形成的 4/5/6 张大小计算支持对子要求，不再默认使用最多张数
- [x] 1.5 确认多个候选招牌动作与碰、吃同屏选择时仍遵守 `hu > ta > zhao > peng > chi`，并允许同一玩家在招牌层级内选择具体招牌大小

## 2. 动作应用与状态

- [x] 2.1 更新服务端 `zhao` 应用逻辑，严格按动作载荷移除指定数量同 key 手牌并形成对应 4/5/6 张明牌组
- [x] 2.2 更新客户端同构规则与本地动画预览，确保预览的明牌组张数与服务端确认一致
- [x] 2.3 确保 `xxxxy + x` 选择招 3 张后，手牌保留剩余 `xy`，可继续参与后续出牌、吃牌或胡牌拆门
- [x] 2.4 检查踏牌逻辑与已有招牌组大小的关系，避免本次变更破坏 `ta` 追加到 4/5 张招牌组的行为

## 3. 真人交互与 AI

- [x] 3.1 设计真人动作弹窗对多个招牌大小的展示方式，例如二级选择或多个 `招4张/招5张/招6张` 按钮
- [x] 3.2 在弹窗中展示每个招牌大小对应的支持对子需求和当前是否满足
- [x] 3.3 更新输入提交，让玩家选择的招牌大小随 `zhao` 意图上报服务端
- [x] 3.4 更新 AI 响应选择：在多个招牌大小之间选择合法且更利于保留门型的选项，至少不得无条件选择最大张数

## 4. 测试与验证

- [x] 4.1 增加自检：`xxx + x` 只生成招 4 张
- [x] 4.2 增加自检：`xxxx + x` 生成招 4 张和招 5 张
- [x] 4.3 增加自检：`xxxxx + x` 生成招 4 张、招 5 张和招 6 张
- [x] 4.4 增加自检：`xxxxy + x` 选择招 4 张后保留 `xy`
- [x] 4.5 增加自检：招 4/5/6 张分别按 1/2/3 对支持对子校验，且 5/6 张要求不同字对子来源
- [x] 4.6 增加自检：支持对子不足时仅过滤对应大小的招牌候选，不影响同一玩家其他合法招牌大小
- [x] 4.7 运行现有花牌规则自检脚本，确认没有破坏吃、碰、踏、胡、出牌限制和计分
- [x] 4.8 运行 `openspec validate support-selectable-zhao-size --strict`
- [x] 4.9 运行 `openspec validate --all --strict`
