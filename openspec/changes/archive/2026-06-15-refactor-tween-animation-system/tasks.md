## 1. Tween.js 接入与基线保护

- [x] 1.1 确认当前微信小游戏构建链可用的 Tween.js 引入方式，固定依赖版本并记录许可证与包体变化
- [x] 1.2 建立最小 Tween.js 运行验证，确认开发者工具和真机均可由现有主循环更新时间、完成动画并清理 Tween Group
- [x] 1.3 为现有牌飞行、停留、归位、本地预演、在线回执和音效去重行为补充回归测试或可重复验证脚本

## 2. 独立动画核心

- [x] 2.1 新建客户端动画模块目录，定义动画方案、动画步骤、视觉对象、播放结果和稳定事件标识的数据结构
- [x] 2.2 实现持有独立 Tween.js Group 的 `AnimationManager`，支持统一 `update(time)`、活动动画查询和视觉状态输出
- [x] 2.3 实现 `tween`、`wait`、`sequence`、`parallel` 和 `effect` 等基础动画步骤编排
- [x] 2.4 实现播放令牌、恰好一次完成通知、取消、替换和 `clear`，确保旧回调不会影响后续动画
- [x] 2.5 为动画管理器的更新时间、缓动、串并行顺序、取消清理和完成一次性语义增加单元测试

## 3. 布局目标与动画预设

- [x] 3.1 从渲染器中提取动画起点、桌前停留点、弃牌区和凑牌区目标解析逻辑，并保持四座位与全面屏安全区域行为
- [x] 3.2 建立游戏事件到声明式动画方案的唯一预设映射层
- [x] 3.3 迁移抓牌、出牌、桌前停留、无人响应归入弃牌区和有人响应归入凑牌区动画
- [x] 3.4 迁移吃、碰、招、踏、胡、过、接庄、不接庄、进圈、流局和结算等已有动作效果
- [x] 3.5 验证迁移后的动画牌保持正确图片比例、现有时长和目标位置，并为主要事件预设增加测试

## 4. 主循环与渲染器解耦

- [x] 4.1 修改游戏主循环，将同一个 `requestAnimationFrame` 时间戳传给动画管理器，并在更新后执行 Canvas 绘制
- [x] 4.2 修改 `TableRenderer`，只读取动画视觉状态并绘制牌、文字和效果，不再自行计算动画进度或完成时机
- [x] 4.3 将尚未迁移的动画接入临时兼容适配，保证同一事件只由新旧路径之一播放
- [x] 4.4 在全部动画迁移完成后删除渲染器内旧手写补间函数、动画状态字段、阶段更新和完成判断

## 5. 在线事件与本地预演对账

- [x] 5.1 将在线权威事件播放改为调用动画管理器统一接口，并在唯一完成通知后发送 `eventSeq` 动画回执
- [x] 5.2 实现本地操作预演的开始、匹配权威事件确认、复用剩余阶段和不匹配或拒绝时取消恢复
- [x] 5.3 保持在线事件序号去重、事件缺口恢复、音效首次触发和回执重试逻辑，移除网络控制器对具体 Tween 或渲染器动画阶段的依赖
- [x] 5.4 将单机状态变化接入同一动画管理器，并移除独立手写动画播放路径
- [x] 5.5 增加测试，验证本地预演确认不重播、取消不回执、重复快照不重复动画或音效、回执只在权威动画完成后发送

## 6. 回归验证与收尾

- [x] 6.1 回归验证四个座位的抓牌、出牌、弃牌、吃、碰、招、踏、胡和结算动画及音效
- [x] 6.2 使用多客户端在线牌局验证动作严格按权威事件顺序播放、动画完成后再推进，且无重复动画、重叠动画或错误停留牌
- [x] 6.3 在微信开发者工具和至少一台全面屏真机验证帧率、内存、包体、图片比例和安全区域表现
- [x] 6.4 移除临时兼容路径与无用旧字段，更新动画模块维护说明并运行项目现有全部测试
