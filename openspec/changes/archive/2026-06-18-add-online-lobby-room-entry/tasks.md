## 1. 服务端房间入口能力

- [x] 1.1 在 `cloudfunctions/game/room.js` 增加查询当前玩家未结束房间的处理函数，返回 `hasRoom`、`roomId`、`seat`、`status`、`version` 和 `settings`
- [x] 1.2 在 `cloudfunctions/game/index.js` 注册新的房间查询 action，并保持现有 `pull`、`createRoom`、`startRound` action 兼容
- [x] 1.3 扩展 `createRoom` 接收 `maxRounds`，校验只允许 `2`、`4`、`6`，缺省使用 `2`
- [x] 1.4 在房间文档保存 `settings.maxRounds`，并为旧房间读取路径提供默认配置
- [x] 1.5 在创建房间前检查当前玩家是否已有未结束房间，避免重复创建

## 2. 客户端在线大厅流程

- [x] 2.1 设计并实现在线大厅状态模型，覆盖 `checking-room`、`reconnecting`、`idle`、`creating` 和 `error`
- [x] 2.2 调整在线入口，使微信登录成功后先显示大厅，而不是直接自动创建房间
- [x] 2.3 在大厅展示玩家昵称和头像，资料缺失时使用登录回退资料
- [x] 2.4 在大厅显示后调用服务端未结束房间查询，查询期间禁用创建房间操作
- [x] 2.5 当查询到未结束房间时，在大厅显示进入房间 loading，并自动执行 `pull`、订阅和进入牌桌
- [x] 2.6 当查询确认无未结束房间时，在大厅显示创建房间按钮
- [x] 2.7 为查询失败、进入房间失败和创建失败提供大厅内错误状态与重试入口

## 3. 创建房间交互

- [x] 3.1 在大厅创建房间流程中提供 `2`、`4`、`6` 局选项
- [x] 3.2 创建房间请求携带玩家选择的 `maxRounds`
- [x] 3.3 创建中显示 loading 并阻止重复点击
- [x] 3.4 创建成功后沿用现有在线牌桌进入流程，保存房间会话并开始订阅牌桌状态

## 4. 验证与回归

- [x] 4.1 增加或更新服务端检查，覆盖无房间、有未结束房间、已关闭房间不返回、非法局数处理和重复创建保护
- [x] 4.2 增加或更新客户端在线流程检查，覆盖登录后进入大厅、有房间自动重连、无房间主动创建和失败重试状态
- [x] 4.3 运行现有在线与服务端核心检查脚本，确认牌桌 `pull`、`roomStates.watch`、心跳和动画回执流程未被破坏
- [x] 4.4 手动验证微信开发者工具中的大厅渲染、局数选择、创建房间、已有房间 loading 重连体验
