## 1. 现状确认

- [x] 1.1 阅读 `js/game/layout.js` 中手牌分列、手牌布局和命中测试相关逻辑，确认当前是否每次按实时手牌重排
- [x] 1.2 阅读 `scripts/run-huapai-checks.mjs` 中现有手牌布局检查，确定可复用的构造数据和断言方式

## 2. 手牌列稳定实现

- [x] 2.1 为我的手牌建立稳定列数据来源，使首次排序后的列身份可以在后续打牌或凑牌后保留
- [x] 2.2 修改打牌后的列更新逻辑，确保非空列即使只剩 1 张也保留原列位置
- [x] 2.3 修改吃、碰、招、踏等凑牌后的列更新逻辑，确保非空列不重新归入最后单牌列
- [x] 2.4 修改空列处理逻辑，确保整列消耗完后移除空列，并让剩余列保持原相对顺序压紧

## 3. 首次分列规则

- [x] 3.1 调整首次排序的最后单牌列判断，只收集真正单张牌
- [x] 3.2 调整首次排序中只剩一个字但数量为 2 张或更多的情况，使这些牌保留为独立牌列
- [x] 3.3 确认每列最多 6 张、同字相邻、底部对齐、列间无额外缝隙等既有布局规则不受影响

## 4. 自动检查

- [x] 4.1 增加初始分列测试，断言最后单牌列不包含对子或多张同字牌
- [x] 4.2 增加打牌后列稳定测试，断言原列剩余 1 张时不移动到最后单牌列
- [x] 4.3 增加凑牌后列稳定测试，断言原列剩余 1 张时不移动到最后单牌列
- [x] 4.4 增加空列压紧测试，断言空列消失且剩余列顺序不变

## 5. 验证

- [x] 5.1 运行 `node scripts/run-huapai-checks.mjs`
- [x] 5.2 运行 `openspec validate stabilize-hand-card-columns --strict`
