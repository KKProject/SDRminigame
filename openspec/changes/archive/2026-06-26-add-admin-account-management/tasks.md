## 1. 后台管理员服务

- [x] 1.1 新增管理员服务模块，实现默认超管初始化、密码哈希校验、会话 token 签发和校验。
- [x] 1.2 支持管理员列表、创建管理员、禁用非默认管理员，并保证敏感字段不返回给前端。

## 2. 后端路由与配置

- [x] 2.1 将管理 API 鉴权从 `ADMIN_TOKEN` 改为管理员会话 token。
- [x] 2.2 新增登录、当前身份、管理员列表、创建管理员、禁用管理员和登出 API。
- [x] 2.3 更新后端配置示例，说明默认超管和后台会话密钥。

## 3. 管理页面

- [x] 3.1 重写 `/admin` 页面为账号密码登录后的可视化工作台。
- [x] 3.2 在工作台中展示房间数据集合计数、单集合/全部清理操作和管理员维护表单。
- [x] 3.3 保持删除操作的 `CLEAR` 二次确认和移动端可用布局。

## 4. 验证与收尾

- [x] 4.1 扩展后端检查脚本，覆盖默认超管、登录鉴权、管理员创建、禁用和房间数据清理。
- [x] 4.2 运行相关后端/在线检查，并通过本地 HTTP smoke test 验证 `/admin` 页面与 API。
- [x] 4.3 同步 OpenSpec 主规格、归档变更并提交本地 commit。
