## 1. 状态生命周期

- [x] 1.1 梳理 `js/game/engine.js` 中 `recentDiscard`、`pendingActions`、`playerActions` 在出牌响应窗口内的清理时机。
- [x] 1.2 确保真人响应弹窗等待期间，`recentDiscard` 保持可用于渲染待响应牌。
- [x] 1.3 调整真人选择吃、碰、招或踏后的状态更新，使最终凑牌结果可被渲染层识别并驱动到凑牌区动画。
- [x] 1.4 调整真人选择过且无人要牌后的状态更新，使最终弃牌结果可被渲染层识别并驱动到出牌玩家弃牌区动画。

## 2. 渲染与动画

- [x] 2.1 检查 `js/game/renderer.js` 的 `updateAnimation`、`updateHeldDiscardAnimation` 和 `shouldHoldRecentDiscard`，确保动作弹窗出现时动画牌保持在出牌玩家前方。
- [x] 2.2 防止待响应牌在等待期间被 `drawDiscardArea` 或 `drawMeldArea` 提前作为 mini 图重复显示。
- [x] 2.3 确保选择凑牌后，动画牌从停留位置移动到真人玩家凑牌区，并在动画结束后由对应凑牌竖列接管显示。
- [x] 2.4 确保选择过后，动画牌从停留位置移动到出牌玩家弃牌区，并在动画结束后由弃牌 mini 图接管显示。
- [x] 2.5 保持动作弹窗绘制与点击区域优先级，不让待响应牌动画影响弹窗操作。

## 3. 验证

- [x] 3.1 增加或更新覆盖真人可凑响应弹窗的测试/自检场景，断言弹窗等待期间打出牌不消失、不提前进入弃牌区或凑牌区。
- [x] 3.2 增加或更新选择凑牌后的验证，确认动画终点为真人玩家凑牌区且最终凑牌列显示该牌。
- [x] 3.3 增加或更新选择过后的验证，确认动画终点为出牌玩家弃牌区且最终弃牌 mini 图显示该牌。
- [x] 3.4 运行现有自检或 smoke 流程，确认出牌动画、动作弹窗和弃牌/凑牌布局没有回归。
