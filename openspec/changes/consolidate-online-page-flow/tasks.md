## 1. 页面状态模型收敛

- [x] 1.1 梳理 `Main.mode` 与 `StartMenu.screen` 当前所有取值和切换入口，标记哪些属于四页模型、哪些是旧占位状态
- [x] 1.2 定义大厅、房间创建、等待页面、游戏页面的唯一状态常量或辅助切换方法
- [x] 1.3 移除或改造 `onLobby` 中阻止回大厅的 `room-ui` guard，确保结束回大厅可覆盖旧创建状态

## 2. 大厅与创建页职责拆分

- [x] 2.1 调整 `renderLobby()`，大厅空闲态只展示玩家资料和进入房间创建页入口
- [x] 2.2 移除大厅内旧的局数选择控件和直接 `create-room` 提交流程
- [x] 2.3 保留房间创建页作为规则设置和确认创建的唯一入口
- [x] 2.4 创建失败时保持在房间创建页并恢复可提交状态

## 3. 等待页和游戏结束落点

- [x] 3.1 创建成功后直接进入等待页面，不再进入或提示旧 `seat-selection` 页面
- [x] 3.2 邀请加入、已有等待房恢复、未开局房刷新继续统一落到等待页面
- [x] 3.3 游戏结束、离开牌桌、拒绝续局、房间关闭和续局决策超时统一落到大厅
- [x] 3.4 回大厅时清理等待页、创建页、旧座位页的忙碌状态、状态文案和触摸残留

## 4. 回归测试

- [x] 4.1 更新 `scripts/run-online-checks.mjs`，覆盖大厅不再渲染旧局数/直接创建按钮
- [x] 4.2 增加创建页确认成功进入等待页、失败停留创建页的测试
- [x] 4.3 增加 `returnToLobby()`、`leaveTable()`、拒绝续局和续局超时后回大厅的测试
- [x] 4.4 增加旧 `seat-selection` 不再作为创建成功落点的测试

## 5. 验证与发布

- [x] 5.1 运行 `node scripts/run-online-checks.mjs`
- [x] 5.2 如触及后端调用兼容，运行 `node scripts/run-backend-checks.mjs`（本次未触及后端调用兼容，无需运行）
- [ ] 5.3 真机验证四页路径：大厅 → 房间创建 → 等待页面 → 游戏页面 → 大厅
- [ ] 5.4 需要发布时，部署后端并上传微信小游戏
