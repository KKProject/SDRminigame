## 1. 现状确认

- [x] 1.1 阅读 `js/game/renderer.js` 中移动动画、按钮绘制、牌绘制和绘制顺序相关逻辑
- [x] 1.2 阅读 `js/game/input.js` 中动作按钮点击入口，确认点击反馈可记录的位置
- [x] 1.3 阅读 `scripts/run-huapai-checks.mjs` 中 fake canvas context 和现有动画断言，确定可复用检查方式

## 2. 动画工具与状态

- [x] 2.1 在 renderer 内增加 `lerp`、`easeOutCubic`、`easeOutBack` 或等价动画工具函数
- [x] 2.2 扩展卡牌动画状态，支持飞行缩放峰值、glow、shadow 和正常尺寸回落
- [x] 2.3 增加短生命周期 effect 列表，用于大字操作特效和按钮反馈
- [x] 2.4 确保动画结束后清理移动动画、effect 和 mini 隐藏状态

## 3. 卡牌飞行与高亮

- [x] 3.1 修改 `drawCardAnimation`，让移动位置使用快入慢出曲线
- [x] 3.2 为飞行中的大牌增加中段放大效果，并在结束时恢复正常尺寸
- [x] 3.3 为飞行中和等待响应的大牌绘制青蓝色高亮描边与柔和 glow
- [x] 3.4 确认大牌宽高比仍保持 big atlas `88x307`

## 4. 操作大字特效

- [x] 4.1 在吃、碰、招、踏动作完成时触发对应大字特效
- [x] 4.2 在胡牌结果出现时触发更醒目的“胡”字特效
- [x] 4.3 在过牌或无人要牌完成时触发短暂“过”反馈
- [x] 4.4 实现大字弹出、回弹、停留、淡出生命周期
- [x] 4.5 确保大字特效绘制在牌层之上、按钮和弹窗之下或不影响按钮命中

## 5. 动作按钮反馈

- [x] 5.1 为新出现的动作按钮增加弹性缩放和淡入效果
- [x] 5.2 为被点击按钮增加短暂缩小或变亮反馈
- [x] 5.3 确保按钮最终命中区域不因视觉缩放而改变
- [x] 5.4 确保按钮反馈不延迟游戏动作执行

## 6. 吃牌组合动画

- [x] 6.1 在吃牌动作确认后触发“吃”字特效
- [x] 6.2 使用最近一次 `layout.handCards` 快照定位参与吃牌的两张手牌
- [x] 6.3 让待响应牌与两张手牌向凑牌区过渡，并短暂组成三张牌组合
- [x] 6.4 找不到手牌原位置时降级为只播放“吃”字和待响应牌到凑牌区动画
- [x] 6.5 确保组合动画期间凑牌 mini 不重复显示正在移动的牌

## 7. 自动检查

- [x] 7.1 增加飞行动画缩放峰值、结束尺寸和 easing 进度测试
- [x] 7.2 增加当前动画牌 glow 绘制调用测试
- [x] 7.3 增加大字 effect 生命周期和绘制顺序测试
- [x] 7.4 增加动作按钮入场与点击反馈状态测试
- [x] 7.5 增加吃牌组合动画状态测试，覆盖有手牌位置和降级路径
- [x] 7.6 保留并更新现有出牌停留、移动到弃牌区/凑牌区、mini 隐藏和安全区动画端点测试

## 8. 验证

- [x] 8.1 运行 `node scripts/run-huapai-checks.mjs`
- [x] 8.2 运行 `openspec validate enhance-card-animation-effects --strict`
- [x] 8.3 运行 `openspec validate --all --strict`
