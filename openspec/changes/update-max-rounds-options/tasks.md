## 1. 后端配置修改

- [x] 1.1 修改 `services/backend/src/game/room.js` 第 38 行的 `SUPPORTED_MAX_ROUNDS` 常量，从 `[1, 2, 4, 6]` 改为 `[8, 16, 24, 32]`
- [x] 1.2 修改 `services/backend/src/game/room.js` 第 39 行的 `DEFAULT_MAX_ROUNDS`，从 `2` 改为 `8`

## 2. 客户端网络层修改

- [x] 2.1 修改 `js/net/online.js` 第 75 行的验证数组，从 `[1, 2, 4, 6]` 改为 `[8, 16, 24, 32]`
- [x] 2.2 修改 `js/net/online.js` 第 75 行的默认回退值，从 `2` 改为 `8`

## 3. 客户端 UI 修改

- [x] 3.1 修改 `js/ui/menu.js` 第 979 行的 checkbox，按钮文案从 `'1局'` 改为 `'8局'`，maxRounds 值从 `1` 改为 `8`
- [x] 3.2 修改 `js/ui/menu.js` 第 980 行的 checkbox，按钮文案从 `'2局'` 改为 `'16局'`，maxRounds 值从 `2` 改为 `16`
- [x] 3.3 修改 `js/ui/menu.js` 第 981 行的 checkbox，按钮文案从 `'4局'` 改为 `'24局'`，maxRounds 值从 `4` 改为 `24`
- [x] 3.4 修改 `js/ui/menu.js` 第 982 行的 checkbox，按钮文案从 `'6局'` 改为 `'32局'`，maxRounds 值从 `6` 改为 `32`

## 4. 测试用例更新

- [x] 4.1 扫描 `scripts/run-online-checks.mjs` 中使用旧的最大局数值（1、2、4、6）的测试数据，进行如下替换：
  - 测试"用户选择 4 局"的用例（第 667、677、682 行附近）：改为使用 `16`
  - 测试"用户选择 6 局"的用例（第 700、701 行附近）：改为使用 `32`
  - 测试"活跃房间"的用例（第 2766、2775 行附近）：改为使用 `16` 或 `8`
  - 测试"重复建房冲突"的用例（第 2860、2878 行附近）：改为使用 `32` 或 `8`
  - 测试"最大轮数"的用例（第 3164 行附近）：改为使用 `8`
  - 其他使用默认值 `2` 的测试数据，可选择改为 `8` 或保持不变（只要验证逻辑不依赖具体数值）

## 5. 验证和测试

- [x] 5.1 本地运行 `node scripts/run-online-checks.mjs`，确保所有测试用例通过，验证后端 API 逻辑正确
- [x] 5.2 本地运行 `npm start`（或相应的启动命令），打开游戏界面验证：
  - 创建房间界面显示四个新的按钮："8 局"、"16 局"、"24 局"、"32 局"
  - 默认选中"8 局"
  - 能够切换按钮并正确更新选择
- [x] 5.3 创建一个测试房间，确认新的最大局数选项能正确保存和显示

## 6. 代码审查和清理

- [x] 6.1 检查是否遗漏其他引用 `SUPPORTED_MAX_ROUNDS` 或硬编码了旧数值的地方
- [x] 6.2 确认所有修改的行号和代码逻辑无误
